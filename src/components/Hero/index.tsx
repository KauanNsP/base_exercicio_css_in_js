import { Form, FormOverlay, FormContent, HeroTitle } from './styles'

const Hero = () => (
  <Form>
    <FormOverlay />
    <FormContent>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
        <FormContent />
      </HeroTitle>
    </FormContent>
  </Form>
)

export default Hero
